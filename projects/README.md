# projects/ — 證據庫（evidence corpus）

每個重要專案／成就一個 Markdown 檔。這裡是「完整版」：背景、做了什麼、數字怎麼來的。
履歷 YAML 裡的每一條 highlight 都應該能對應回這裡的某一段，
Claude 針對職缺客製時只能從這裡「挑選與改寫」，不能新增這裡沒有的事實。

建議欄位（見 order-service-migration.md）：
- 時間、公司、角色
- 問題 / 背景
- 我做了什麼（可條列）
- 結果與數字（附量測方式或來源）
- 用到的技術
- 可用的關鍵字（方便對應 JD）
